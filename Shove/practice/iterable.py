def all(iterable):
    element = int(input('Enter many element'))
    element = 0
    for element in iterable:
        print("element")
        if not element:
            return False
    return True
